<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ndtech.peryahanngbayan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".CLAIM.ClaimMainActivity"></activity>
        <activity android:name=".VOID.VoidBetMainActivity" />
        <activity android:name=".SALES.SalesMainActivity" />
        <activity android:name=".PRINTER.PrinterMainActivity" />
        <activity android:name=".HITS.HitsMainActivity" />
        <activity android:name=".HISTORY.HistoryMainActivity" />
        <activity android:name=".BET.NewBetMainActivity" />
        <activity android:name=".MainMenu.MainMenuActivity" />
        <activity android:name=".Login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>